## Outcome

Make the required TestRail driver safe to use for the complete release, including effectful report GET endpoints.

Implement within the accepted local-stdio design: one TestRail identity per process; one tool per endpoint; every operation enabled by default, including writes/admin/delete; no server-added confirmation. TestRail 10.7.0 is the full-coverage baseline; older versions are best effort.

Architecture: [accepted design](https://github.com/dichovsky/testrail-mcp/blob/codex/testrail-mcp-plan/docs/architecture.md). Exact engineering contract: [implementation contracts](https://github.com/dichovsky/testrail-mcp/blob/codex/testrail-mcp-plan/docs/implementation-contracts.md). [Endpoint inventory](https://github.com/dichovsky/testrail-mcp/blob/codex/testrail-mcp-plan/docs/operation-inventory.json).

## Dependencies

None. This item can begin independently.

Tracking epic: [full TestRail MCP release](https://github.com/dichovsky/testrail-mcp/issues/1).

## Scope

- Start deterministic development on exact @dichovsky/testrail-api-client@7.0.0. Confirm the inventory at source 89f636e276ea701412bb06039e3b963d83126ea1 matches the published endpoint surface; do not treat unpublished main runtime fixes as part of 7.0.0.
- Track upstream inclusion of the network-guard fixes in client-core.ts and config-validation.ts after release commit 71a80d984aea14713d8eeaf6ac9a0d41c1fba12b.
- Implement or obtain an upstream fix in reports.runReport and reports.runCrossProjectReport: preserve GET routing, ID validation and ReportResultSchema; add retry: none and bypassCache: true so both cache and request coalescing are bypassed.
- Qualify an exact published version containing both changes, update dependency/lockfile and inventory provenance, and rerun available adapter contracts. Complete all 133-endpoint adapter qualification in R01 after the tool families are implemented. Never use a floating Git branch, private request API or separate MCP HTTP workaround.
- Record upstream issue/PR/release links here as they exist. Adapter development can proceed while this external prerequisite is open; T11 qualification and R03 release cannot.

## Acceptance and verification

- [ ] Published tarball integrity and public exports are recorded; all 133 endpoint methods and 48 helpers remain available. Any endpoint/parameter difference is reconciled explicitly.
- [ ] Driver tests prove sequential and concurrent report calls each trigger a distinct fetch, regardless of caching settings, with zero retries on network, 429 and 5xx failures.
- [ ] Both report methods retain schema advisory behavior and ID validation; the regression tests run against the installed published artifact, not only a sibling checkout.
- [ ] The final dependency contains the network-guard regression tests/fixes and passes the supported runtime checks. Attach the exact version and verification evidence.

Execution order and release gates: [implementation plan](https://github.com/dichovsky/testrail-mcp/blob/codex/testrail-mcp-plan/docs/implementation-plan.md). This issue describes work to implement and verify; it is not a record of completed implementation.
